@model List<RelatorioAlunosDto>

<table class="table">
    <thead>
        <tr>
            <th scope="col">Nome</th>
            <th scope="col">Cpf</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            foreach (RelatorioAlunosDto aluno in Model)
            {
                <tr>
                    <td>@aluno.Aluno.Nome </td>
                    <td>@aluno.Aluno.Cpf </td>
                </tr>
            }
        }
    </tbody>
</table>

<div class="text-center">
    <a class="btn btn-primary" type="button" asp-controller="Home" asp-action="Index"> Voltar</a>
</div>